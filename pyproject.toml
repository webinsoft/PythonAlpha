[project]
name = "pythonalpha"
version = "0.1.0"
description = "FastExpr to Python Alpha pipeline"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.0.0",
    "pandas>=2.0.0",
    "requests>=2.30.0",
]
